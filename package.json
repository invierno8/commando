{
  "name": "hangar-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix data",
    "dev": "concurrently -n client,data -c blue,green \"npm run dev --prefix client\" \"npm run dev --prefix data\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
