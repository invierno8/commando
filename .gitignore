node_modules
client/node_modules
client/dist
data/node_modules

# live "database" — real, gitignored, files created lazily & empty (see FORCLAUDE.md)
data/db/
# real secrets (ADMIN_SECRET, host config) — never committed; see data/.env.example
data/.env
data/.env.local

.DS_Store

# Claude Code local tooling scratch space (e.g. isolated agent worktrees) — never real repo content
.claude/
